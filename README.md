# text-to-ASCII-art-generator
two simple scripts made to turn text into ascii art format with desired font

Dependencies:
ruby and python with their respective libaries should be installed for each one

for ruby:

sudo apt update && sudo apt-get install ruby-full
gem install artii

for python:

sudo apt update && sudo apt install python3 python3-pip
pip install pyfiglet


